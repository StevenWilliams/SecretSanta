name: ${project.name}
version: ${project.version}
main: ${project.groupId}.${project.artifactId}.${project.name}
description: |
    ${project.description}
authors: [puppy3276]
commands:
  secretsanta:
    description: Give a gift!
    permission: secretsanta.participate
  secretsantatest:
    description: internal debug command
    permission: secretsanta.admin
